# alphaflux-web

The marketing site at `alphaflux.net`. Static HTML served by nginx, generated by a Go program in this repository.

## How it is built

```bash
go run ./build
```

That reads `content/*.json` and writes the pages to the repository root, plus `sitemap.xml`, `robots.txt`, `llms.txt` and `llms-full.txt`. The generated HTML is committed, so the container build needs no Go toolchain and the served site is plain static files.

## Layout

| Path | What it is |
|---|---|
| `content/*.json` | One file per page. The source of truth for all copy |
| `build/` | The generator. Go, standard library only |
| `assets/site.css` | The token layer and every component |
| `assets/fonts.css`, `assets/fonts/` | Self-hosted faces, Latin and Latin Extended subsets |
| `assets/icons.svg` | The icon sprite. Every icon is referenced by id from content |
| `assets/og/` | Open Graph card sources, rendered to PNG by `scripts/make-og.sh` |
| `*.html`, `sitemap.xml`, `robots.txt`, `llms.txt` | Generated. Edit the content or the generator, not these |
| `default.conf` | nginx: clean URLs via `try_files`, cache policy, admin path passthrough |

## Adding a page

1. Add a `content/<slug>.json` file.
2. Add it to the matching group in `Site` in `build/main.go` so it appears in the rail, the footer and the sitemap.
3. Run `go run ./build`.

The build fails on a rail entry with no page, a duplicate slug, or an icon referenced in content that is not in the sprite.
